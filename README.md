E-Commerce Order & Returns – Java Console Application

How to Compile
Open your terminal and navigate to the project root:

bash
cd path/to/ECommerceOrderReturns
Compile all Java files:

bash
javac -d out src/com/ecommerce/*.java

How to Run
Run the application from the compiled output directory:

bash
java -cp out com.ecommerce.Main
